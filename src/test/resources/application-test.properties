spring.application.name=covoit-app

## MySQL - Base de données de test
spring.datasource.url=${DB_URL_COVOIT_TEST}  ##   TEST !!
spring.datasource.username=${DB_USER_COVOIT}
spring.datasource.password=${DB_PASS_COVOIT}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuration Hibernate pour les tests
spring.jpa.hibernate.ddl-auto=update

# JWT Configuration (même que production)
jwt.cookie=AUTH-TOKEN
jwt.expires_in=7200
jwt.secret=secretsecretsecretsecretsecretsecretsecretsecretsecret

# Configuration Mailjet (utilise les mêmes clés que production)
mailjet.email.expediteur=covoit@goegilles.fr
mailjet.email.nom.expediteur=Covoit
mailjet.template.id=7318767
mailjet.api.key.public=${MJ_APIKEY_PUBLIC}
mailjet.api.key.private=${MJ_APIKEY_PRIVATE}

# URL de base pour les tests
app.base.url=http://localhost:8080